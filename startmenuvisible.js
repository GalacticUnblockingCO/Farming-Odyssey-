// Function to display the start menu
function displayStartMenu() {
    // Get reference to the start menu
    const startMenu = document.getElementById('startMenu');

    // Display the start menu
    startMenu.style.display = 'block';
}
